create-messages-table=CREATE TABLE IF NOT EXISTS messages (id SERIAL PRIMARY KEY, sender VARCHAR(255), content TEXT)
create-message=INSERT INTO messages (sender, content) VALUES ($1, $2)
all-messages=SELECT * FROM (SELECT * FROM messages ORDER BY id DESC LIMIT 20) sub ORDER BY id ASC;
